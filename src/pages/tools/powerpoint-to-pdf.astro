---
import ToolLayout from "~/layouts/ToolLayout.astro";
import { TOOL_FAQS } from "~/data/tool-faqs";
import { TOOL_PROSE } from "~/data/tool-prose";

const slug = "powerpoint-to-pdf";
const title = "PowerPoint to PDF — free, on-device | oriz / pdf-tools";
const description = "Convert presentations to PDF documents";
const faqs = TOOL_FAQS[slug] ?? [];
const proseHTML = TOOL_PROSE[slug] ?? "";
---

<ToolLayout slug={slug} title={title} description={description} faqs={faqs}>
	<Fragment set:html={proseHTML} />
</ToolLayout>

<script>
	import { createElement } from "react";
	import { createRoot } from "react-dom/client";
	import PowerPointToPDF from "../../tools/PowerPointToPDF";
	const m = document.getElementById("tool-mount");
	if (m) createRoot(m).render(createElement(PowerPointToPDF));
</script>
