---
import ToolLayout from "~/layouts/ToolLayout.astro";
import { TOOL_FAQS } from "~/data/tool-faqs";
import { TOOL_PROSE } from "~/data/tool-prose";

const slug = "add-page-numbers";
const title = "Add Page Numbers to PDF — Free & No Upload";
const description = "Insert customizable page numbers into your PDF. Choose position, format, and starting number. Free, private, browser-based.";
const faqs = TOOL_FAQS[slug] ?? [];
const proseHTML = TOOL_PROSE[slug] ?? "";
---

<ToolLayout slug={slug} title={title} description={description} faqs={faqs}>
	<Fragment set:html={proseHTML} />
</ToolLayout>

<script>
	import { createElement } from "react";
	import { createRoot } from "react-dom/client";
	import AddPageNumbers from "../../tools/AddPageNumbers";
	const m = document.getElementById("tool-mount");
	if (m) createRoot(m).render(createElement(AddPageNumbers));
</script>
