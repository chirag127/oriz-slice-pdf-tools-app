---
import ToolLayout from "~/layouts/ToolLayout.astro";
import { TOOL_FAQS } from "~/data/tool-faqs";
import { TOOL_PROSE } from "~/data/tool-prose";

const slug = "word-to-pdf";
const title = "Word to PDF Online — Free & No Upload";
const description = "Convert DOCX Word documents to PDF format. Fast, free, private — all processing in your browser.";
const faqs = TOOL_FAQS[slug] ?? [];
const proseHTML = TOOL_PROSE[slug] ?? "";
---

<ToolLayout slug={slug} title={title} description={description} faqs={faqs}>
	<Fragment set:html={proseHTML} />
</ToolLayout>

<script>
	import { createElement } from "react";
	import { createRoot } from "react-dom/client";
	import WordtoPDF from "../../tools/WordtoPDF";
	const m = document.getElementById("tool-mount");
	if (m) createRoot(m).render(createElement(WordtoPDF));
</script>
