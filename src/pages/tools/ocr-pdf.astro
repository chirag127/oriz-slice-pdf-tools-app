---
import ToolLayout from "~/layouts/ToolLayout.astro";
import { TOOL_FAQS } from "~/data/tool-faqs";
import { TOOL_PROSE } from "~/data/tool-prose";

const slug = "ocr-pdf";
const title = "OCR PDF Online — Free & No Upload";
const description = "Make scanned PDFs searchable with OCR. Extract text from images. Free, private, browser-based.";
const faqs = TOOL_FAQS[slug] ?? [];
const proseHTML = TOOL_PROSE[slug] ?? "";
---

<ToolLayout slug={slug} title={title} description={description} faqs={faqs}>
	<Fragment set:html={proseHTML} />
</ToolLayout>

<script>
	import { createElement } from "react";
	import { createRoot } from "react-dom/client";
	import OCRPDF from "../../tools/OCRPDF";
	const m = document.getElementById("tool-mount");
	if (m) createRoot(m).render(createElement(OCRPDF));
</script>
