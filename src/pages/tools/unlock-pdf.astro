---
import ToolLayout from "~/layouts/ToolLayout.astro";
import { TOOL_FAQS } from "~/data/tool-faqs";
import { TOOL_PROSE } from "~/data/tool-prose";

const slug = "unlock-pdf";
const title = "Unlock PDF Online — Free & No Upload";
const description = "Remove password protection from PDF files. Enter the password to unlock and download an unprotected copy. Free, private, browser-based.";
const faqs = TOOL_FAQS[slug] ?? [];
const proseHTML = TOOL_PROSE[slug] ?? "";
---

<ToolLayout slug={slug} title={title} description={description} faqs={faqs}>
	<Fragment set:html={proseHTML} />
</ToolLayout>

<script>
	import { createElement } from "react";
	import { createRoot } from "react-dom/client";
	import UnlockPDF from "../../tools/UnlockPDF";
	const m = document.getElementById("tool-mount");
	if (m) createRoot(m).render(createElement(UnlockPDF));
</script>
